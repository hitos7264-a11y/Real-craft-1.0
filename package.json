{
  "name": "hrgm-voxel-world",
  "version": "1.0.0",
  "private": true,
  "description": "WebGPU voxel world — optional Android APK wrapper",
  "scripts": {
    "icons": "python make_icons.py",
    "cap:init": "npm install && npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
